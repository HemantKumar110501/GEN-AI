# GIT useful Commands(CLI):-

1. git init
2. git add f1.md
3.1. git rm --cached f1.md  (for undo from A to U  AND DELETING ITS HISTORY FROM .git)
3.2. git  restore --staged f1.md  (for undo from A to U AND NOT DELETING ITS HISTORY FROM .git)

#commit k liye
4. git commit -m "First Commit"
5. git commit --amend -m "New Commit Message"  (isse edit hota h commit with mesaage)
5.1 git commit --amend --no-edit    (edit hota h commit with no message)

#rename branch
1. git branch -m newbranchname (respectedly changing branch name)
1. git branch -M  newbranchname (forcefully changing branch name)


# To connect local repository(GEN AI 💻) to remote repository( GEN-AI github folder) 
1. git remote add origin https://github.com/HemantKumar110501/GEN-AI.git

# to upload our code to github.
2. git push -u origin main (for first time)
2. git push origin main (for non-first time)


